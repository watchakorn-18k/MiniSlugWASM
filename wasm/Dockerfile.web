# Lightweight Web Server for MiniSlug WASM
FROM nginx:alpine

# Copy custom Nginx config (ensures WASM MIME type is correct)
COPY nginx.conf /etc/nginx/nginx.conf

# Copy static files from the build directory
# (Assumes you have already run ./wasm/build.sh)
COPY build /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]
